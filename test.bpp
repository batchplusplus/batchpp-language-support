@echo off

# This is a comment

defmacro greet name
echo Hello %name%!
endmacro

callmacro greet John

nmap --version >nul 2>&1

if %errorlevel% == 0
    echo Nmap is installed
else
    echo Nmap is not installed
endif
